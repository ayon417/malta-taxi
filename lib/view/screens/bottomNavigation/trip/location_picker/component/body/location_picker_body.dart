import 'package:flutter/material.dart';
import 'package:malta_taxi/utils/colors.dart';

class LocationPickerBody extends StatelessWidget {
  const LocationPickerBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: kWhite,
    );
  }
}
